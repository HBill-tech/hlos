## 时钟中断和实时时钟中断的区别

#### 硬件不同

* PIT 中断：操作的是 PIT (8253)，通过端口 0x40-0x43 编程。
* RTC 中断：操作的是 RTC (实时时钟)，通过 CMOS 寄存器（端口 0x70-0x71）编程。

#### 目的不同

* PIT 中断（通常频率更高，如 100Hz）是操作系统的心跳，用于任务调度、时间片轮转等核心功能。
* RTC 中断（通常频率较低，如 4Hz）主要用于维护系统实时时钟（年月日时分秒），也可以用于唤醒、闹钟等。

[返回README](../README.md)